# Copy to .env and set values. Required on EC2 for docker-compose.prod.yml.
# On server: cp .env.example .env  then edit .env (use same strong password for both below).

MSSQL_SA_PASSWORD=YourStrongPasswordHere!
DB_USER=sa
DB_PASSWORD=YourStrongPasswordHere!
# SQL Server requires 8+ chars, upper, lower, digit, symbol. DB_ENCRYPT is set in compose.
